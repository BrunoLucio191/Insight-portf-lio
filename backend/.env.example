NODE_ENV=development
PORT=3000

# Gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_REFRESH_SECRET=

# Gerar: node -e "import('bcryptjs').then(({default:b})=>b.hash('SUA_SENHA',12).then(console.log))"
ADMIN_PASS_HASH=

ALLOWED_ORIGINS=http://localhost:5173
CSRF_SECRET=
LOG_LEVEL=info
